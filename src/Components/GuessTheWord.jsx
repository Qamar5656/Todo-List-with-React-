// Filter todos by search query
// const filteredTodos = todos.filter((todo) =>
//   todo.text.toLowerCase().includes(searchQuery.toLowerCase())
// );
import React from "react";

const GuessTheWord = () => {
  return <div>GuessTheWord</div>;
};

export default GuessTheWord;
